# Egui Reader Rendering Parity Checks

This document records the reader rendering parity checks to run during migration.

## Parity checks
- Validate click-to-play behavior matches current reader semantics.
- Validate search navigation behavior matches current reader semantics.
- Validate highlight sync and settings behavior match current reader semantics.
