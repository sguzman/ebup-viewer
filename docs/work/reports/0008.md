# Goal 0008 report — Caliberate service integration

## Result

Implemented and validated first-class Caliberate HTTP/JSON v1 support behind the existing native Rust library browser. The legacy Calibre content-server path remains available through an explicit provider boundary. All acceptance criteria passed; the goal is ready for director review/integration.

## Provider Boundary

Added `CalibreProvider::{Caliberate, Calibre}` to `CalibreConfig`, with Caliberate as the new default and `http://127.0.0.1:8181` as the default service target. Existing configuration files without a provider remain compatible: legacy URLs containing a library fragment infer the legacy Calibre provider. Catalog, materialization, authentication, thumbnails, and cache identity dispatch through the provider boundary.

## Caliberate Catalog Mapping

Added the v1 `/api/v1/books` adapter. It maps IDs, titles, authors, publication years, primary formats, and supported format metadata into the existing `CalibreBook` model. Unsupported rows are skipped; supported formats use deterministic extension priority and fall back to `primary_format` when needed.

## Pagination

Requests use bounded pages (`offset`, `limit=500`, title ascending sort) and continue until the reported total/items are exhausted. Cancellation is checked before page requests and while mapping rows.

## Content Materialization

Supported content is fetched from `/api/v1/books/{id}/content/{format}`. Bytes are written to a provider-specific temporary file, flushed, and atomically renamed into the existing download cache. Caliberate content is isolated under the `caliberate` download-cache namespace.

## Authentication

Caliberate API keys are sent only as `x-api-key` headers. Legacy Basic authentication remains limited to the legacy Calibre provider. API keys and passwords are excluded from diagnostics and cache identity.

## Legacy Calibre Compatibility

The existing `/interface-data/books-init` catalog and `/get/{FORMAT}/{id}/{library_id}` materialization routes remain intact behind the Calibre provider. Legacy config loading and Basic authentication are covered by regression tests.

## Thumbnail Behavior

Caliberate never probes legacy `get/thumb/{id}` or `get/cover/{id}` routes. Local sidecars and embedded EPUB covers remain available. Provider identity is included in thumbnail cache scope.

## Cache / Persistence

Catalog cache identity now includes provider identity, preventing Caliberate and legacy catalogs from colliding. Thumbnail and content cache scopes are likewise provider-isolated. No persistence format migration was introduced.

## Diagnostics

Catalog diagnostics identify provider, base URL, page offsets, and counts without logging secrets. Unreachable Caliberate services return explicit actionable errors naming Caliberate and the requested endpoint context.

## Tests

- `cargo fmt --all -- --check` was run; it reports pre-existing formatting differences in unrelated repository files. Focused rustfmt checks for all touched Rust files pass.
- `cargo check --workspace` passed.
- `cargo build --workspace` passed.
- `cargo test --workspace` passed.
- Caliberate-focused library tests passed 11/11, including pagination, mapping, format priority/fallback, API-key isolation, content bytes, atomic materialization, unreachable-service diagnostics, cache identity, and legacy-thumbnail suppression.
- `cargo test -p lanternleaf-core --test non_pdf_parity -- --nocapture` passed.
- `git diff --check` passed.

## Windows CI

GitHub Actions run [34183204993](https://github.com/sguzman/lantern-leaf/actions/runs/34183204993) passed both `native-workspace` and `hosted-renderer-probe`. Native validation included Windows check, build, workspace tests, repo-native QA preparation, goal-notification workflow tests, and the Windows TTS probe.

## Remaining Human Verification

After director integration, the human maintainer should pull `main`, ensure the local Caliberate service is running at `127.0.0.1:8181`, and use the repo-native loop `git pull -> .\\qa.ps1 -> test -> report observations`. Confirm catalog loading, opening a supported Caliberate book, and the existing reader/TTS flow. Hosted CI cannot verify the human machine's local service or desktop GUI behavior.

## Recommended Next Goal

Human desktop verification and any follow-up parity fixes discovered while exercising a populated Caliberate library through `qa.ps1`.

## Git

- Branch: `codex/0008-caliberate-service-integration`
- Implementation commit: `c65c7ef feat(library): add Caliberate provider adapter`
- Activation commit: `a361179 chore(work): activate goal 0008`
- Terminal state: goal moved from `active/` to `done/` in the terminal commit below.
- Goal watcher was launched for this execution attempt; terminal signaling occurs after the terminal push.

## Correction History — Attempt A2

Director review identified two defects in the first attempt: stale-signature fallback could return the other provider's catalog, and legacy Calibre HTTP compatibility lacked an end-to-end regression. The correction preserved the accepted provider implementation and repaired both items.

- Catalog cache files are now explicitly scoped by provider and cache signature, and the serialized cache also records provider identity. Signature-mismatch fallback is accepted only for the same provider.
- Added a deterministic stale-fallback test proving a legacy cache is not returned to an unreachable Caliberate provider, while same-provider stale fallback remains available.
- Added a local mock-server regression proving legacy `/interface-data/books-init?...library_id=...` catalog retrieval, row mapping, `/get/EPUB/{id}/{library_id}` materialization, returned bytes, and Basic authentication.
- Focused provider tests: 13 passed / 0 failed.
- `cargo check --workspace`, `cargo build --workspace`, and `cargo test --workspace` passed. The workspace core suite reached 180 passed / 0 failed.
- Touched Rust files remain rustfmt-clean and `git diff --check` passes. Broad repository formatting still has the previously documented unrelated drift.
- Correction implementation commits: `a53c6b8` and `bf366f8`.
- Fresh authoritative Windows CI run [34185172624](https://github.com/sguzman/lantern-leaf/actions/runs/34185172624) passed both `native-workspace` and `hosted-renderer-probe`.

## Correction History — Attempt A3

Real-desktop evidence showed that catalog population and EPUB materialization were not enough to prove the reader handoff. This correction closes that blind spot without changing the provider or reader architecture.

- Caliberate tests now serve a deterministic, syntactically valid EPUB and exercise materialization, `epub_loader::load_book_content`, and the normal `session::load_session_for_source` path. Assertions cover known canonical TTS text, structured/native reading HTML, and canonical session sentences.
- EPUB source loading now uses one native `EpubDoc`/native HTML pass and derives TTS text with existing Rust-native `html2text`; EPUB opening no longer calls the unbounded external Pandoc process.
- Caliberate content downloads use the existing 10-minute content HTTP timeout. Existing EPUB cache paths are validated for non-empty/readable spine content; invalid entries are removed, downloaded once again, validated, and fail explicitly if the replacement remains invalid.
- Added deterministic cache-recovery coverage for both successful poisoned-cache replacement and still-invalid replacement cleanup.
- Added stage-level tracing for EPUB cache hit validation, downloaded-byte validation, native EPUB parse, native text extraction, and completion. Existing `materializing` and `loading_reader` progress remains in the egui effect path; failures propagate through the existing source-open failure handling.
- A3 focused Caliberate/provider suite: 15 passed / 0 failed.
- `cargo test -p lanternleaf-core --test non_pdf_parity -- --nocapture`: passed.
- Normal-parallel `cargo test --workspace`: passed, including 182 core tests / 0 failures and the existing egui/app suites.
- `cargo check --workspace`, `cargo build --workspace`, focused rustfmt checks, and `git diff --check`: passed. Repository-wide rustfmt retains unrelated pre-existing drift.
- Fresh authoritative Windows CI run [34253400879](https://github.com/sguzman/lantern-leaf/actions/runs/34253400879) passed both `native-workspace` and `hosted-renderer-probe`, including Windows check/build/test and the Windows TTS probe.

## Correction History — Attempt A4

Real-desktop evidence localized the remaining Goal 0008 defect to reader startup: a one-page EPUB entered synchronous whole-book normalization before the reader became visible. A4 changes the canonical session/TTS path rather than adding a Caliberate-specific workaround.

- `load_session_for_source_with_cancel` no longer performs all-page normalization precompute. Source ingestion, repagination, and sentence-anchor setup return the reader session without waiting for TTS normalization.
- Idle reader snapshots use cheap canonical sentence metadata and do not construct a normalization/TTS plan. TTS plans are built lazily in a bounded 64-display-sentence window around the active sentence; crossing a window reuses the existing cursor/mapping contract and prepares the next bounded window.
- Sentence-mode normalization retains deterministic sentence-addressable cache reuse, but only the active bounded window is populated synchronously; opening a large book no longer creates thousands of cache files before reader visibility.
- Stable abbreviation regexes/tokens, brand/custom pronunciation patterns, acronym matchers, and year matching are compiled once when `TextNormalizer` is constructed. Invalid configured abbreviation regexes continue to be warned and skipped safely.
- Added stage timing diagnostics for repagination/anchor setup, bounded normalization-plan preparation, reader snapshot creation, and existing TTS batch preparation logs.
- Added `large_book_open_and_first_tts_use_only_a_bounded_normalization_window`, a deterministic 3,500-sentence canonical-session regression proving idle open has no plan/cache work, first TTS is bounded, cache population is bounded, and 100 next-sentence transitions preserve cursor progress across plan-window boundaries.
- Focused core suite: 183 passed / 0 failed. Non-PDF parity: 2 passed / 0 failed. Simulated non-PDF TTS runtime: 1 passed / 0 failed.
- Serial `cargo test --workspace -- --test-threads=1` passed all workspace suites, including 183 core, 67 app, 30+30 egui, non-PDF parity/TTS, persistence, pipeline, Windows TTS probes, pretty parity, shell, and doctests. A parallel run reproduced the repository's existing shared-cache race in `list_recent_books_prunes_stale_recent_entry_dirs`; the exact test passed serially and no unrelated cache code was changed.
- `cargo check --workspace`, `cargo build --workspace`, targeted rustfmt, and `git diff --check` passed. Repository-wide `cargo fmt --all -- --check` still reports unrelated pre-existing formatting drift outside the A4 files.
- Fresh authoritative Windows CI run [34260546736](https://github.com/sguzman/lantern-leaf/actions/runs/34260546736) passed both `native-workspace` and `hosted-renderer-probe`, including Windows check/build/test and the Windows TTS probe.

## Correction History — Attempt A5

Real-desktop evidence showed that A4's acceptable source-open latency still left the native egui frame path cloning the complete 104k-book catalog and large reader payloads, rebuilding all 1,644 pretty blocks every frame, and synchronously applying TTS commands twice on the egui thread. A5 repairs those ownership and bounded-work violations without adding a Caliberate-specific reader or TTS state machine.

- `AppState` now owns the large catalog and immutable reader document snapshot through `Arc`; the egui projection clones state without copying 104,732 book DTOs or the reader payload. Persistence lifecycle state retains only source identity instead of a full per-frame `ReaderSnapshot`. A deterministic large-state regression asserts `Arc::ptr_eq` for catalog and reader payloads after cloning.
- Pretty HTML/Markdown blocks are still parsed once per cache identity, but native rendering now uses a viewport-derived window with eight-block overscan. A cached normalized sentence-to-block index preserves off-screen highlight lookup without scanning/layout-building every block each frame; jump-to-highlight selects a bounded target window. Slow-frame, projection, bounded-window, and render timing diagnostics were added.
- TTS commands from egui are submitted to a dedicated `lanternleaf-tts-control` worker. The TTS runtime is the single command/cursor authority for those commands; the duplicate effect-session application is skipped for TTS commands, and lightweight playback/TTS deltas update the app state. High-frequency TTS events no longer carry reader snapshots. A deterministic 10,488-sentence regression proves submission returns within 50 ms, the worker starts, and emitted events carry no full snapshot.
- Added repo-owned `[profile.qa]` (`opt-level = 2`, debuginfo retained) and changed default `qa.ps1` builds to that profile; `-Release` remains available. The vendored eSpeak target-directory lookup now supports Cargo custom profiles whose `PROFILE` inherits `release`. Optimized Windows QA profile build and repo-native `qa.ps1 -SkipDependencyCheck -PrepareOnly` both passed.
- Focused A5 regressions passed: large Arc-backed UI state, bounded 1,644-block pretty window, and 10,488-sentence worker-owned TTS submission. Serial `cargo test --workspace -- --test-threads=1` passed the complete workspace: 183 core, 69 app, 31+31 egui, non-PDF parity/TTS, persistence, pipeline, Windows TTS, pretty parity, shell, and doctests.
- `cargo check --workspace` passed; targeted rustfmt and `git diff --check` passed. Repository-wide `cargo fmt --all -- --check` still reports unrelated pre-existing formatting drift outside A5.

Windows CI for the pushed A5 implementation is the remaining automated gate before terminalizing this correction. No human QA was requested or performed during A5.
