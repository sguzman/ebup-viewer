# Dual-View Ingestion CI Verification

This document captures the CI verification requirement for dual-view ingestion and TTS pipeline work.

## Requirement
- Build, tests, and the Tauri appimage compile path pass in CI (excluding `deb`/`rpm` targets).
