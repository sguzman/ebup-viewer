[package]
name = "lanternleaf-app"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
lanternleaf-core = { path = "../lanternleaf-core" }
serde = { version = "1", features = ["derive"] }
tracing = "0.1"
ts-rs = { version = "10.0.0", features = ["serde-compat"] }
