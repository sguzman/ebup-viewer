# Goal 0006 Director Review — Correction Required

## Decision

**DO NOT INTEGRATE YET. CONTINUE THE SAME GOAL 0006.**

The first Goal 0006 pass produced useful fixtures, a real loader/session integration test, a parity matrix, a manual QA checklist, and a green Windows baseline. However, it does not satisfy the explicit parity acceptance gates in the authorized goal.

This is a bounded correction pass, not a new numbered macro-goal.

Preserve:

- repository macro-goal ID: **0006**;
- branch lineage: `codex/0006-non-pdf-reader-parity`;
- report lineage: `docs/work/reports/0006.md`.

The first Codex Goal UI session has already terminated. Start/reset a **fresh Codex Goal session** for this correction attempt.

This fresh session is **not** repository Goal 0007. It is Attempt 2 of repository Goal 0006.

Before editing, read this review and the reopened goal from current `main`, continue the existing implementation branch, synchronize the director-owned correction/governance state into that branch safely, and re-arm the watcher for the new attempt.

## Accepted work from the first pass

Keep:

- representative TXT / Markdown / HTML fixtures;
- deterministic EPUB builder;
- `ReaderSession::load_session_for_source` use;
- current source/session invariants that are genuinely asserted;
- simulated TTS real-session coverage already present for TXT/Markdown;
- manual Windows QA checklist;
- parity matrix structure;
- green Windows CI evidence;
- terminal notification workflow.

Do not rewrite these merely because a correction is required.

## Finding 1 — search parity is overstated

The matrix claims:

`Search next/previous/selected result = verified automated`

for all four formats.

The new real-session test only issues:

`SearchSetQuery`

and checks that matches are non-empty.

It does not exercise:

- search next;
- search previous;
- selected-result movement/identity.

### Required correction

For representative real sessions, exercise the actual session commands for:

- set query;
- next match;
- previous match.

Assert that the selected result changes deterministically and maps to the expected canonical sentence/page position.

Update the matrix to reflect exactly what is proven.

## Finding 2 — canonical-text cleanliness is claimed but not asserted

The report says the tests verify syntax-clean canonical text, but the shown contract only proves:

- canonical sentences exist;
- sentence splitting is internally consistent.

It does not explicitly reject representative source noise.

### Required correction

Add source-family-specific assertions, using the representative fixtures:

### Markdown

Assert canonical speech text does not contain representative raw syntax such as:

- heading markers;
- emphasis delimiters;
- raw link/image syntax;
- list markers where the canonicalizer is expected to remove them.

Do not write brittle global assertions that reject legitimate punctuation.

### HTML

Assert canonical speech text does not expose:

- HTML tags;
- script/style content;
- representative attributes/markup tokens.

### EPUB

Assert canonical speech text does not expose:

- XHTML/XML tags;
- package/chapter markup tokens.

If current production behavior fails these expectations, fix the bounded canonicalization defect rather than weakening the assertion.

## Finding 3 — pretty projection / anchor evidence is too shallow

Goal 0006 explicitly required current Rust-native pretty projection evidence for Markdown / HTML / EPUB.

The first pass checks:

- `PrettyKind`;
- sentence-anchor-map cardinality;
- presentation toggle preservation.

It does not actually inspect representative pretty structures or test mapping quality.

### Required correction

Use the current Rust-native pretty/block helpers and representative fixtures to prove supported structures are present/readable.

At minimum cover the structures that current production code claims to support from the fixtures:

- heading;
- paragraph;
- emphasis;
- list;
- link;
- image;
- HTML table/secondary block where current model supports it.

For sentence-to-pretty mapping, test:

- at least one exact/strong mapping;
- at least one deterministic bounded fallback when exact mapping is unavailable, if the current model exposes such a path;
- no unrelated/random block selection;
- presentation toggle preserves canonical/search state.

Do not invent support for structures the model genuinely degrades. Mark those as degraded in the matrix with evidence.

## Finding 4 — auto-scroll / jump semantics are not newly proven

The matrix claims deterministic mapping logic is automated for Markdown/HTML/EPUB, but the Goal 0006 diff adds no obvious targeted auto-scroll/jump decision test.

### Required correction

Locate the current pure decision logic used to decide active-sentence jump/scroll behavior.

Add deterministic tests where feasible for:

- new active sentence triggers a jump;
- same sentence does not repeatedly retrigger jitter;
- mapping/fallback strength affects jump behavior as intended.

If the logic is inseparable from egui and cannot be tested without GUI state, document the exact seam and mark the matrix **manual verification required** instead of **verified automated**. Do not claim automation that does not exist.

## Finding 5 — TTS runtime parity covers only TXT/Markdown

The authorized goal states:

> Use the simulated TTS runtime with sessions loaded from the real representative fixtures.

and asks for each format to cover a meaningful subset.

The first pass only loads:

- `representative.txt`;
- `representative.md`

into `TtsRuntime`.

HTML/EPUB are not exercised through the simulated runtime.

### Required correction

On Pandoc-capable hosted Windows:

- load representative HTML real session;
- load deterministic EPUB real session;
- run them through simulated `TtsRuntime`.

For all four formats, cover a sensible common subset including at least:

- PlayFromPageStart;
- PlayFromHighlight;
- SeekNext;
- SeekPrev;
- RepeatSentence;
- Stop/cancellation.

Assert:

- canonical cursor remains in range;
- active sentence text agrees with canonical cursor where exposed;
- stale cancellation cannot mutate the replacement request/cursor;
- backend setting changes do not fork reader index semantics.

You may parameterize this instead of duplicating tests.

## Finding 6 — delete/cache idempotence is overstated

The report says deletion is asserted idempotent.

The new representative-session test calls:

`delete_recent_source_and_cache(path)`

once.

That is not an idempotence test.

### Required correction

For representative sources, call cleanup at least twice and assert both calls succeed with stable postconditions.

Where HTML/EPUB assets have special cleanup behavior, verify that bounded contract or mark remaining manual asset cleanup explicitly.

## Finding 7 — Windows source -> synthesis acceptance needs explicit disposition

Goal 0006 acceptance gate 13 requires either:

- a representative source -> session -> Windows synthesis -> decode probe; or
- a report explanation for why the existing generic Windows synthesis probe adds equivalent evidence and the extra path would not materially improve it.

The first report does not clearly make that determination.

### Required correction

Preferred: add a Windows-only integration probe that:

1. loads a representative non-PDF source through the real loader/session path;
2. obtains one canonical sentence;
3. synthesizes it through the Windows backend;
4. decodes the WAV.

TXT is sufficient for this contract; the goal is to connect **source/session canonical ownership** to actual Windows synthesis without requiring Pandoc or physical audio.

If you choose not to add it, the report must make a concrete technical case for why it is redundant.

## Finding 8 — parity matrix must be evidence-accurate

The current matrix contains several cells marked `verified automated` more strongly than the new tests support.

### Required correction

After the correction pass:

- every `verified automated` cell must point to actual current test evidence;
- use `manual verification required` where only GUI/runtime observation remains;
- use `known degraded behavior` where the current pretty model intentionally degrades;
- do not use existing generic tests as proof of format-specific behavior unless the test genuinely constructs/loads that format.

## Required validation

Run:

```powershell
cargo check --workspace
cargo build --workspace
cargo test --workspace
git diff --check
```

Normal parallel execution remains authoritative.

Hosted Windows/Pandoc must execute the HTML/EPUB correction paths.

## Handoff

Continue on the same branch:

`codex/0006-non-pdf-reader-parity`

Update the existing report rather than replacing its history. Add a clearly named section:

`## Director Correction Pass`

Summarize each finding above and the exact evidence that closes it.

Only move Goal 0006 to `done/` after these corrected acceptance gates pass.

Use the normal post-push terminal notification protocol again. Launch `scripts/codex-goal-notify.ps1` with `-Rearm` at correction-attempt activation so the first attempt's `.terminal` / `.ack` state cannot trigger the new watcher.

## Stop / escalation

Do not stop for ordinary failures inside these correction items.

Escalate only if:

- current pretty/anchor architecture cannot express deterministic mapping without a new director-level semantics choice;
- source-specific canonicalization expectations conflict materially across formats;
- adding HTML/EPUB simulated runtime coverage requires changing canonical reader ownership;
- source -> Windows synthesis reveals a cross-layer architectural contradiction.


## Director acceptance after correction

**ACCEPTED.**

Correction attempt 2 closes the eight findings above without changing the canonical reader/session architecture.

Verified final implementation branch:

`codex/0006-non-pdf-reader-parity`

Final head reviewed:

`d06d5a3d30231d9cc355bc3aaf5710d717d3d05a`

Authoritative Windows correction run:

`34159728934`

Verified evidence:

- required Windows `cargo check --workspace` passed;
- required Windows `cargo build --workspace` passed;
- normal-parallel `cargo test --workspace` passed;
- core unit tests reached 171 passed / 0 failed;
- real-session non-PDF parity tests passed on the Pandoc-capable Windows runner;
- all-format simulated non-PDF TTS parity passed, including HTML and EPUB;
- native Markdown/HTML/EPUB pretty projection tests passed;
- source -> real TXT session -> canonical sentence -> WinRT synthesis -> Rodio decode passed;
- generic Windows synthesis still passed with the installed Microsoft David voice;
- renderer capability remained separately classified.

The correction now proves:

1. real-session search set/next/previous selection behavior;
2. source-family canonical-text cleanliness;
3. representative native pretty structures and bounded anchors;
4. deterministic auto-scroll duplicate/fallback decisions;
5. simulated TTS runtime coverage across TXT, Markdown, HTML, and EPUB;
6. twice-invoked idempotent source/cache cleanup;
7. source/session-to-Windows-synthesis continuity;
8. an evidence-accurate parity matrix.

The implementation branch diverged from `main` only because director-owned Codex-session continuation/watcher doctrine changed while correction attempt 2 was running. GitHub therefore could not merge the branch directly. The accepted code/tests were transplanted onto current `main` lineage by the director rather than asking the human/Codex to perform a third correction attempt.

Automated Gate 2 parity is complete. Real desktop GUI/audio/interaction signoff is still required before Gate 2 is fully closed.
