# Implementation Prioritization Scope Note

This document captures the scope note for implementation prioritization.

## Scope note
- The roadmap prioritizes the highest-risk, largest subsystems (runtime, shell, reader, PDF, audio, persistence) with instrumentation and tracing baked in.
- It does not enumerate every small refactor, localization tweak, or UX polish; treat those as follow-on tasks once the tranches close.
